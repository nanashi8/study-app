# えいごクエスト（study-app）で作業するときの決まり

## 依頼の完了の決まり（最優先）

このプロジェクトでは、依頼を途中で止めて「完了」と報告することを仕組みで禁じている。
2026-09-21、ほかの品詞の形を表示する依頼で、次の4つの失敗が重なった。決まりはそれぞれの対策になっている。

| 起きた失敗 | 原因 | 対策（決まり） |
| --- | --- | --- |
| 「英単語で別の品詞の形を表示」なのに、つづりの規則で拾えた単語だけを対象にし、全体の53%で完了と報告した | 範囲を依頼の文ではなく、作りやすさで決めた | 受け入れ条件ごとに**母集団**（例「品詞のある全見出し語 8,799語」）を依頼の文から書く。作りやすい部分集合に狭めない |
| テストが緑なのに依頼は終わっていなかった | 完了を自分が作った物差し（拾った候補を全部決めたか）で判定した | 条件ごとに**母集団の全件を確かめるコマンド**（check）を書く。done にできるのは check が通るときだけ |
| 学習に役立つ形やつづりの似た別語を、黙って捨てた | 依頼の目的（学習の助け）より、自分の判断で絞った | 候補を捨てる箱を作らない。外すなら理由を台帳に書き、報告で明示する |
| 辞書ページに熟語の欄がないこと、不規則な変化形の熟語がつながっていないことを確かめなかった | 変更した画面しか見なかった | 依頼に関わる**すべての画面・データ**を条件に挙げる（暗記カード・辞書ページ・テスト・一覧など） |

### 依頼台帳（requests/*.json）

利用者の依頼を受けたら、**コードを変える前に** `requests/<YYYY-MM-DD>-<名前>.json` を作る。

```json
{
  "id": "2026-09-21-word-relations",
  "title": "何をする依頼か",
  "asked": ["利用者の依頼の原文をそのまま（追加の指示も全部）"],
  "status": "open",
  "criteria": [
    {
      "id": "forms-all-words-reviewed",
      "text": "満たすべきこと",
      "population": "対象の母集団（全件の範囲）",
      "check": "満たしたかを確かめるコマンド（通れば満たした）",
      "status": "todo"
    }
  ]
}
```

- `status` は `todo` / `done` / `waiting`（裏の処理待ち。`waitingFor` に何を待つか）/ `needs-user`（利用者の判断待ち。`question` に聞くこと）。
- 依頼を閉じる（`status: "done"`）ときは、各条件の check を `tests/*.test.mjs` にして npm test で守り続ける。
- 途中で利用者から追加の指示が来たら、原文を `asked` に足し、条件を増やす。

### フック（.claude/settings.json）が強制すること

- **ターンを終えられない**：開いている依頼に `todo` の条件が残っていると、Stop フックが止めて作業を続けさせる。
- **台帳なしに変更できない**：開いている依頼がないと、`src/`・`tests/`・`scripts/` を Edit/Write できない。
- **偽りの done をコミットできない**：`git commit` / `git push` の前に、done にした条件の check を実行し、通らなければ止める。
- **毎回思い出させる**：利用者の発言ごとに、決まりと未完了の条件を作業者に見せる。

判定は `scripts/check-requests.mjs`。一覧は `node scripts/check-requests.mjs --report`。

### 報告の書き方

- 条件ごとに「母集団と達成数」を数字で書く（例「8,799語中8,799語を確認」）。
- やっていないこと、範囲を狭めたこと、外したものは必ず書く。未完了のまま「完了」と書かない。
- 回答・進捗・ツールの説明文は日本語で書く。

## そのほかの決まり

- main に直接コミットして push する（ブランチ・PR は作らない）。push 前の最終確認はクリーンな clone で行う。
- 変更は `npm run audit:all-content`（全テスト・全監査・監査台帳の書き直し）を通してからコミットする。
