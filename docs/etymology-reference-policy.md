# 語源資料を安全に参照する方針

## 目的

語源辞典・学習書は、既存データの見落としを発見するための調査資料として使う。収録するのは、複数の資料や既存語源メモで確認できる「語の由来・語根・意味変化」という事実だけとする。

## 収録してよいもの

- 語根の形、元の言語、基本義などの事実
- 同語源であることを確認できた、アプリ収録語どうしの関係
- 上の事実を、中高生向けにアプリ独自の順序と文章で説明したもの
- 書名・著者・出版社・刊行年など、参照履歴に必要な書誌情報

## 収録しないもの

- 本文、見出し、例文、語呂合わせのコピーまたは近い言い換え
- イラスト、図、色分け、ページ構成、章立て、項目の選択・配列
- PDF、ページ画像、OCR全文、索引全文
- 綴りが似ているだけの語や、資料間で由来が割れる語の断定的な同根リンク

## 追加手順

1. 資料からは「確認候補」だけを一時的に抽出する。
2. 全語彙と照合し、既存語源メモまたは独立した語源資料で関係を再確認する。
3. 同綴り・別語源を避けるため、補助語根リンクは見出し語の完全一致許可リストにし、既存の自動語根リンクも別語源の除外テストを通す。
4. 説明文は資料を閉じた状態で、事実から新しく書く。
5. `referenceRoots` は辞書の関連語表示だけに使い、既存の語源カードID、`etymologySrs`、進捗コード、クラウド同期を変えない。
6. 全語彙監査、誤接続テスト、変更前後のカード割当比較を通す。
7. 調査用PDF・画像・OCRはリポジトリへ入れず、作業後に破棄する。

## 今回の参照履歴

- 清水建二・すずきひろし著、本間昭文イラスト『英単語の語源図鑑』、かんき出版、2018年
- 参照目的: 既存語源リンクの漏れ候補を発見すること
- 実装: 書籍の文・図・例文・配列を保存せず、既存語の関係と不足見出しを独自の意味・例文・語源説明としてデータ化

## 2026年8月17日の全件監査

- 索引は一時画像を行単位で再確認し、1,019見出しを抽出した。
- 変更前の8,211語には804見出しがあり、不足は215見出しだった。
- 不足215語を独立見出しとして追加し、変更後は1,019 / 1,019見出しを収録した。
- 103の代表見出しも103 / 103を収録した。
- 調査用画像・OCR・索引一覧はリポジトリへ保存せず、集計とアプリ固有のコンテンツだけを残す。
- 既存8,211語・既存2,678語源パックは変更前後でIDと割当が一致し、補完語は `completion:` 名前空間へ分離した。
- 補助語根462リンクを既存461語の通常の語源説明と関連語に表示し、補完語との語族109組は新旧両側からたどれるようにした。

## 2026年9月5日の再監査（語源カードの拡張）

語源を「暗記・テスト・一覧」で学べる教材にするため、公開カードの母数を作り直した。

- 形態素分解（`derive-roots.js` の接頭辞＋語根）で得た候補を1語ずつ辞書見出しで確認し、
  残した967リンク（106語根）を [`etymology-morpheme-audit.js`](../src/data/etymology-morpheme-audit.js) に固定した。
  別系統だった語（`telemedicine`＝medicus、`dialect`＝ギリシャ語 legein、
  古フランス語 poser 系の `-pose` 動詞）は理由つきで `REJECTED_MORPHEME_LINKS` に残す。
- 紐づく語が1語だけの語根（`scope`）は、関連語を示せないためカードにしない。
- 英検・大学入試で効く古典語根40件を [`etymology-classical-roots.js`](../src/data/etymology-classical-roots.js) に追加し、
  259リンクを明示リストで結んだ。綴りの自動推測（`autoRootIds`）の対象にはしない。
- 台帳 [`etymology-card-reviews.js`](../src/data/etymology-card-reviews.js) を196枚へ広げ、
  各カードの語根説明・全紐づけ語・照合見出しを固定するSHA-256を取り直した。

| 母数 | 変更前 | 変更後 |
| --- | ---: | ---: |
| 公開カード | 109枚 | 196枚 |
| 紐づく単語（一意） | 781語 | 1,977語 |
| カード→単語リンク | 786件 | 2,019件 |
| 照合先URL | 404件 | 752件 |

## 法的な基準

文化庁は、単なる事実・データ・アイデア・ありふれた表現は著作物に当たらない一方、創作的な文章・図・素材の選択や配列は保護対象になり得ると説明している。本文等を引用する場合は、著作権法32条・48条に従い、必要性、明確な区分、正当な範囲、出所明示を個別に満たす。通常の語源データ追加では引用を行わない。
